---
description: Prefer mature third-party libs; do not reinvent wheels
alwaysApply: true
---

# 优先成熟第三方，不重复造轮子

本仓库工程约定：

1. **能用成熟开源包就用**：通用工具、编排、校验、存储等，优先选社区广泛使用、维护活跃、类型完善的库（如 `es-toolkit`、`zod`、LangGraph、Cordis），不要手写等价实现。
2. **自研只留给差异化**：产品边界、架构红线（纯 Node 能力包、沙箱策略、赛事评分闭环）才值得自建；`clamp` / `debounce` / `chunk` / 深拷贝等一律用 `es-toolkit`（或文档已定案的替代）。
3. **选型先写理由**：新增依赖记入 `docs/phase1/04-依赖选型.md`；若否决某个成熟方案而自研，必须在决策笔记写清原因。
4. **禁止无必要重复**：仓库内已有能力包 API（如 `@chatvein/agents`）时，app/CLI 只调用，不另写一套循环或网关。
