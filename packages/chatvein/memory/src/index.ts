/**
 * @chatvein/memory
 *
 * Structured layered memory: recall() / write() / consolidate() pipelines
 * for personal, group and global scopes (CP2). See docs/design/03-记忆方案.md.
 */

export const CHATVEIN_MEMORY_VERSION = '0.1.0'
