/**
 * code worker（coder_task）。
 *
 * `createDeepAgent` 尚未接线（阶段 B）：母图 `agentic·code` 当前降级为 general
 * worker 执行（见 `lanes/agentic`）。persona 已内化，接线后直接挂上即可。
 */
export { DEFAULT_CODER_SYSTEM_PROMPT } from './prompt'
