fastapi>=0.115,<1.0
uvicorn>=0.32,<1.0
pydantic>=2.9,<3.0
sqlmodel>=0.0.22,<2.0
# BM25 全文检索。numpy 由 bm25s 自动带入，pip 会按 Python 版本挑兼容版本，
# 无需在此固定（dev / 打包 / 类型检查三处运行时已统一锁定为 Python 3.13）。
bm25s>=0.3,<1.0
# 向量检索。sqlite-vec 是 SQLite 的可加载扩展，随 wheel 自带 vec0 二进制（约 300KB），
# 复用主库连接、无需独立向量库进程/目录；相比 lancedb（连带 pyarrow 上百 MB）
# 能显著压缩打包体积。db.py 在每个连接上加载该扩展，业务侧直接建 vec0 虚拟表。
sqlite-vec>=0.1,<0.2
# Windows 上 zoneinfo 需要 IANA 数据库（get_current_time / convert_time）。
tzdata>=2024.1
langgraph>=1.2,<2.0
langchain>=1.0,<2.0
langchain-openai>=1.6,<2.0
# DeepSeek thinking + 工具多轮需回传 reasoning_content；见 agents/llm.py ChatDeepSeekReact。
langchain-deepseek>=1.1,<2.0
# 本地向量模型：ONNX Runtime 推理，不引入 torch。模型权重不随安装包分发，
# 首次使用时按需下载到 CHATVEIN_DATA_DIR/embeddings（见 embeddings/service.py）。
fastembed>=0.8,<1.0
# Agent 联网搜索降级链（均可无 key）：
# Claw Search（Parallel Free MCP）→ Tavily Keyless → Firecrawl Keyless → ddgs。
# 抓取：Firecrawl → httpx/bs4。可选 env：TAVILY_API_KEY / FIRECRAWL_API_KEY / CLAW_SEARCH_MCP_URL。
# 见 mcps/tools/web.py。
ddgs>=9.0,<10
beautifulsoup4>=4.12,<5
lxml>=5.0,<7
# Browser 自动化（mcp-browser）：进程内 Playwright，工具对齐 @playwright/mcp。
# 浏览器二进制不随包装；需本机 `playwright install chromium`（见 docs/agents.md）。
# aria_snapshot(mode="ai") 需较新版本（约 >=1.59）。
playwright>=1.59,<2
# IP 归属地（mcp-ip）：免费降级 ipinfo → ipwhois-python → ip-api（均无 Key）。
ipwhois-python>=1.2,<2
# PDF 处理（mcp-pdf）：pypdf 做读取/合并/拆分/加解密（纯 Python、轻量）；
# reportlab 做文本生成 PDF（内置 CID 中文字体，无需字体文件）。见 mcps/tools/pdf.py。
pypdf>=5.0,<7.0
reportlab>=4.0,<5.0
# 办公三件套（mcp-docx / mcp-pptx / mcp-excel）：纯 Python 直接生成 OOXML，
# 无 MS Office / LibreOffice 依赖；工具实现在 mcps/tools/{word_tools,pptx_tools,excel_tools}.py。
python-docx>=1.1,<2.0
python-pptx>=1.0,<2.0
openpyxl>=3.1,<4.0
