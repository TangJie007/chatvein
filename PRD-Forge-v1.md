# PRD：Forge —— 自研 Agent Harness 桌面应用（第一期 / 初赛版）

## 1 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-09-03 |
| 作者 | 唐杰 |
| 评审人 | 待定 |
| 状态 | 草稿 |
| 项目代号 | Forge（锻造炉） |
| 目标赛事 | GOSIM 智能体软件工厂黑客松（OAIC 主办）初赛 |
| 技术栈 | Electron（上层 GUI）+ LangChain.js + LangGraph.js（编排）+ TS/Node.js |
| 官网 | https://create.gosim.org/factory26/ |

## 2 需求摘要

| 项 | 内容 |
|----|------|
| 功能名称 | Forge：面向黑客松初赛的软件开发智能体（Agent Harness）桌面应用 |
| 功能背景 | 赛事要求参赛队交付一个"能自动把需求文档变成可运行软件的智能体"，由机器按测试通过率、Token 用量、完成时间三项自动评分；需要一套可控、可观测、可复现的 Harness 来提升这三项指标 |
| 目标用户 | 参赛者本人（开发者）及 1–3 人小队 |
| 核心场景 | 导入需求文档 → 一键启动 → Agent 自动完成"理解需求 → 拆分任务 → 写代码 → 跑构建与测试 → 修复 → 提交"，全程可在桌面端观测与干预 |
| 业务目标 | 初赛进入排行榜 Top 20，晋级 10/1–10/7 大奖赛 |
| 优先级 | P0（赛事主线） |
| 期望完成时间 | 2026-09-20（研习营结束前达到可参赛状态） |

## 3 背景与目标

### 3.1 背景

| 项 | 说明 |
|----|------|
| 赛事机制 | 参赛队交付一个软件开发智能体；主办方提供统一模型网关、统一沙箱、统一黑盒 GUI 测试用例 |
| 初赛赛题 | GitHub + Spreadsheets 功能复刻：Actions、组织权限、审计、Rulesets |
| 评分方式 | 机器自动采集三项：① GUI 测试用例通过率（通过数/总数）② Token 效率（模型网关计量）③ 完成时间（墙钟 + git 时间戳）。权重待官方公布 |
| 参赛路径 | 官方给出五条参考路径：自研 Agent、Octos、HAgency、Claude Code、Codex |
| 奖金与权益 | 奖金池 24,000 美元（特等 2×3000 / 一等 4×2000 / 二等 6×1000 / 三等 8×500）；Top 20 晋级大奖赛；练习期每日约 ¥200 Token 额度 |
| 关键时点 | 研习营 9/7–9/20（六讲课程 + 三场答疑）｜初赛 9/21–9/30｜大奖赛 10/1–10/7｜颁奖 10/17 深圳 |

### 3.2 目标

1. 交付一个**可复现**的软件开发智能体：同一份需求文档输入，多次运行的产出质量与 Token 消耗可预期、可对比。
2. 交付一个**可观测**的运行控制台：每一次模型调用、工具调用、人工干预都留痕，支持事后复盘与归因。
3. 交付一个**可信**的验证闭环：产出必须通过构建、自测、静态检查三重独立验收，而非模型自述"已完成"。
4. 在三项评分指标上跑赢官方给出的 Claude Code / Codex 基线（基线值在 M0 阶段实测获得）。

### 3.3 成功指标

| 指标 | 定义 | 第一期目标 | 采集方式 |
|------|------|-----------|---------|
| 黑盒测试通过率 | 主办方 GUI 测试用例通过数 / 总数 | ≥ 85% | 赛事平台反馈 |
| 自测通过率（本地代理指标） | 本地冒烟用例通过率 | ≥ 90% | Forge 内置验证器 |
| Token 效率 | 单次完整任务消耗的 token 总量 | ≤ 基线 × 0.8（基线由 M0 用 Claude Code 实测） | 模型网关 / Forge 计量面板 |
| 完成时间 | 需求导入到产出自测通过的墙钟耗时 | ≤ 基线 | Forge 计时器 |
| 可复现性 | 同一输入重复运行 3 次，通过率标准差 | ≤ 5% | Forge 回归脚本 |
| 无人值守成功率 | 无人干预跑完全流程的比例 | 100% | 运行记录 |
| 可观测完整性 | 每次运行 trace 事件落盘率 | 100%，无缺失 | trace 文件校验 |

## 4 用户与场景

### 4.1 用户画像

| 角色 | 特征 | 诉求 |
|------|------|------|
| 参赛开发者（主要用户） | 9 年前端经验，TS/Node 技术栈，熟悉 LangChain.js | 用熟悉的技术栈控制住 Agent，不依赖 Python 生态 |
| 调试者 | 需要定位"Agent 为什么写错了" | 完整的 trace、可回放、可对比两次运行差异 |
| 调优者 | 需要在 Token / 时间 / 质量之间取舍 | 参数面板 + 成本实时可视化 + A/B 对照 |

### 4.2 核心使用场景

| 场景 | 触发条件 | 用户行为 | 预期结果 |
|------|---------|---------|---------|
| 场景 1：一次完整交付 | 拿到赛事需求文档 | 导入文档 → 配置模型与沙箱 → 点击"开始锻造" → 挂机 | 无人干预跑完，输出可运行系统 + 完整报告 |
| 场景 2：研习营练习调优 | 练习赛题 | 改参数（模型分级/并行度/上下文策略）→ 重跑 → 对比两次运行的三项指标 | 面板显示差异，定位是质量掉分还是 Token 超支 |
| 场景 3：卡死归因 | 运行中途失败或 token 飙升 | 打开 trace 时间线 → 定位异常步骤 → 看该步的完整工具输入输出 | 定位到具体是哪个工具调用导致跑偏，形成一条规则沉淀 |
| 场景 4：人工干预（练习期） | Agent 反复无法修复同一错误 | 在控制台注入人工提示 / 手动改文件 → 继续运行 | Agent 接管人工改动继续推进，干预留痕 |
| 场景 5：回归基线 | 每次改动 Harness 后 | 跑回归脚本（固定需求 + 固定 seed） | 输出三项指标对比表，确认未退化 |

## 5 功能需求

### 5.1 功能清单

| 模块 | 功能点 | 优先级 | 备注 |
|------|--------|--------|------|
| M1 运行控制台 | 需求导入与运行启动 | P0 | 支持文件路径 / 拖拽 |
| M1 运行控制台 | 实时运行总览（阶段、当前任务、耗时、Token） | P0 | |
| M1 运行控制台 | Trace 时间线（模型调用 / 工具调用 / 事件） | P0 | 可展开查看原始输入输出 |
| M1 运行控制台 | 成本面板（按模型 / 按节点 / 按任务统计 Token） | P0 | 直接服务评分指标 |
| M1 运行控制台 | 人工干预入口（注入提示 / 暂停 / 终止 / 重跑单步） | P0 | 练习期核心能力 |
| M1 运行控制台 | 两次运行对比视图 | P1 | 调优期使用 |
| M1 运行控制台 | 参数配置面板（模型、并行度、预算上限、重试上限） | P0 | |
| M2 需求编译 | 需求文档解析（Markdown / 文本） | P0 | 赛事文档为 DSL 形式 |
| M2 需求编译 | 任务树生成（结构化、带验收标准） | P0 | 产出可被 LangGraph 消费的 task list |
| M2 需求编译 | 任务依赖与并行分组 | P1 | 影响完成时间指标 |
| M3 编排 | LangGraph 状态机（plan→implement→verify→diagnose→fix→integrate） | P0 | |
| M3 编排 | Checkpoint 与断点续跑 | P0 | 崩溃后不重头来（保护时间指标） |
| M3 编排 | 任务级并行（多子任务并发子图） | P1 | 时间指标杠杆，但有 Token 风险 |
| M3 编排 | 全局预算护栏（Token 上限 / 步数上限 / 时间上限） | P0 | 硬性熔断 |
| M4 工具层 | 文件读写与补丁（read / write / patch） | P0 | |
| M4 工具层 | 代码检索（glob / grep / 符号索引） | P0 | 减少无效读取，省 Token |
| M4 工具层 | Shell 执行（npm install / build / test） | P0 | 带超时与输出截断 |
| M4 工具层 | Git 操作（init / commit / diff） | P0 | 提交时间戳是评分采集依据 |
| M4 工具层 | 服务启动与冒烟探测 | P1 | 本地自测用 |
| M5 上下文与 Token | 工具输出截断（头尾保留 + 中间折叠） | P0 | Token 指标核心 |
| M5 上下文与 Token | 历史压缩（阶段性摘要） | P0 | Token 指标核心 |
| M5 上下文与 Token | Prompt 缓存复用 | P1 | 视网关支持情况 |
| M5 上下文与 Token | 模型分级路由（强/中/弱模型按节点分配） | P0 | Token 与质量双向杠杆 |
| M6 验证闭环 | 构建校验 | P0 | |
| M6 验证闭环 | 自测执行与解析（失败用例结构化） | P0 | |
| M6 验证闭环 | 静态检查（lint / 类型检查） | P1 | |
| M6 验证闭环 | 失败归因与定向修复（只回传关键错误帧） | P0 | |
| M6 验证闭环 | 重试上限与降级策略 | P0 | 防止死循环烧 Token |
| M7 模型网关 | OpenAI 兼容适配器（统一 baseURL） | P0 | 赛事网关统一接入 |
| M7 模型网关 | 多模型切换与降级 | P0 | 限流/超限时切备用模型 |
| M8 沙箱 | 工作区隔离（独立目录 + 子进程执行） | P0 | |
| M8 沙箱 | 容器化隔离（Docker / WSL2） | P1 | 与赛事沙箱对齐 |
| M9 可观测 | Trace 事件落盘（JSONL） | P0 | |
| M9 可观测 | 运行报告导出（Markdown 汇总） | P1 | 提交与复盘 |
| M10 提交 | 产物打包与提交检查清单 | P0 | 防止漏交 |

### 5.2 系统分层

| 层 | 职责 | 技术选型 | 说明 |
|----|------|---------|------|
| 表现层 | GUI、可视化、人工干预 | Electron + React + TS | 主进程跑 Agent，渲染进程做 UI，IPC 双向通信 |
| 编排层 | 状态机、任务调度、Checkpoint | LangGraph.js | @langchain/langgraph + checkpointer |
| 模型层 | 模型接入、流式调用、工具绑定 | LangChain.js | @langchain/core、@langchain/openai（兼容网关） |
| 工具层 | 文件 / Shell / Git / 检索 | LangChain Tool 抽象 + Node fs/child_process | 每个工具强制超时与输出截断 |
| 沙箱层 | 执行隔离、资源限制 | 子进程（P0）/ Docker 或 WSL2（P1） | 与赛事统一沙箱行为对齐 |
| 数据层 | Trace 落盘、任务树、运行记录 | 本地文件（JSONL + JSON） | 不引数据库，降低复杂度 |

### 5.3 功能详情

#### 5.3.1 运行控制台（M1）

**需求描述：** 桌面端主窗口，承载从"导入需求"到"查看结果"的完整操作闭环。所有运行过程必须可见、可中断、可回放。

**布局分区：**

| 区域 | 位置 | 内容 | 优先级 |
|------|------|------|--------|
| 顶部工具条 | 顶部 | 运行控制（开始/暂停/终止/重跑）、运行 ID、状态徽标、耗时计时 | P0 |
| 左侧任务树 | 左栏 | 需求编译产出的任务树，标注每个任务状态（待办/进行中/通过/失败/跳过） | P0 |
| 中间 Trace 时间线 | 中栏（主区） | 按时间顺序的事件流：模型调用、工具调用、验证结果、错误、人工干预 | P0 |
| 右侧诊断面板 | 右栏 | Token 统计（总量/本步/按模型）、错误摘要、当前文件路径、预算进度条 | P0 |
| 底部日志 | 底部 | 沙箱 stdout/stderr 原始输出（可折叠、可搜索） | P1 |

**交互流程：**

1. 用户选择需求文档路径 → 系统解析并预览任务树（此时不消耗模型调用，仅本地解析）。
2. 用户配置运行参数（模型分配、并行度、预算上限）→ 点击"开始锻造"。
3. 主进程启动 LangGraph 运行，通过 IPC 推送事件流到渲染进程，时间线实时滚动。
4. 运行中出现失败 → 右栏高亮错误摘要 → 用户可注入提示或手动修改文件后继续。
5. 运行结束 → 弹出运行报告（三项指标 + 失败清单 + trace 文件路径）。

**异常处理：**

| 异常场景 | 处理方式 |
|---------|---------|
| 需求文档解析失败 | 顶部提示，展示解析错误位置，不启动运行 |
| 模型网关超时 / 限流 | 自动降级到备用模型，时间线标注降级事件，重试上限 3 次 |
| 单步执行超过预算 | 触发熔断，暂停并提示用户选择"继续"或"终止" |
| 子进程崩溃 | 记录退出码与最后 200 行输出，从最近 Checkpoint 恢复 |
| 应用被关闭 | 下次启动检测未完成运行，提示从 Checkpoint 续跑 |

**埋点需求（本地记录，不上传）：**

| 事件 | 触发时机 | 关键属性 |
|------|---------|---------|
| run_start | 运行开始 | run_id、需求文档 hash、模型配置、参数 |
| node_enter / node_exit | 进入/退出图节点 | 节点名、耗时、累计 token |
| tool_call | 每次工具调用 | 工具名、参数大小、返回大小、耗时、是否截断 |
| verify_result | 每次验证 | 通过数、失败数、失败用例 ID |
| human_intervene | 人工干预 | 干预类型、注入内容长度 |
| run_end | 运行结束 | 总耗时、总 token、最终状态、失败原因 |

#### 5.3.2 需求编译（M2）

**需求描述：** 将赛事给定的需求文档（DSL 形式）转换为结构化的任务树，每个任务带明确的验收标准，供编排层消费。**这是"一次理解对"的关键节点，直接影响返工次数与 Token 消耗。**

**任务数据结构：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 任务唯一标识 |
| title | string | 任务标题 |
| requirement_ref | string[] | 回溯到需求文档原文章节，保证可追溯 |
| acceptance | string[] | 验收标准（可验证的断言式描述） |
| depends_on | string[] | 依赖任务 ID，用于拓扑排序 |
| parallel_group | number | 并行分组，同组任务可并发 |
| estimated_complexity | low / mid / high | 用于模型分级路由 |
| status | pending / running / passed / failed / skipped | 运行时状态 |

**处理流程：**

1. 本地预处理：切分文档、建立章节索引（不消耗模型调用）。
2. 模型调用：按章节抽取功能点与验收标准，输出结构化 JSON。
3. 校验：JSON Schema 校验失败则重试一次，仍失败则该章节降级为单任务。
4. 拓扑排序 + 并行分组 → 任务树落盘，供控制台展示与用户微调。

**异常处理：** 文档过大超过上下文 → 按章节分批抽取再合并；抽取结果为空 → 告警并提示人工补充。

#### 5.3.3 LangGraph 编排（M3）

**需求描述：** 用 LangGraph 定义主状态机，节点职责单一，边条件明确，支持 Checkpoint 与并行子图。

**状态 Schema：**

| 字段 | 类型 | 说明 |
|------|------|------|
| run_id | string | 运行标识 |
| workspace_path | string | 沙箱工作目录 |
| requirement_path | string | 需求文档路径 |
| task_tree | Task[] | 需求编译产出 |
| current_task_ids | string[] | 当前处理的任务（并行时多个） |
| file_index | Record<string, string> | 已读文件摘要，避免重复读取 |
| build_status | unknown / pass / fail | 最近一次构建结果 |
| test_report | TestResult | 最近一次自测结果（结构化失败项） |
| root_cause | string | 失败归因结论 |
| retry_count | number | 当前任务重试次数 |
| token_usage | TokenStat | 累计 Token（按模型分类） |
| trace_events | TraceEvent[] | 事件流（落盘为 JSONL） |
| budget | Budget | 预算上限与已用 |
| status | running / paused / done / aborted | 运行状态 |

**节点定义：**

| 节点 | 职责 | 输入 | 输出 | 建议模型档 |
|------|------|------|------|-----------|
| plan | 需求编译，产出任务树 | 需求文档 | task_tree | 强 |
| dispatch | 选取下一批可并行任务 | task_tree、已完成状态 | current_task_ids | 无模型调用 |
| implement | 按任务写代码 / 改文件 | 任务 + 相关文件上下文 | 文件变更 | 强 |
| verify | 构建 + 自测 + 静态检查 | 工作区 | build_status、test_report | 无模型调用 |
| diagnose | 失败归因，定位根因与责任文件 | 失败报告 | root_cause | 中 |
| fix | 定向修复（只改责任文件） | root_cause + 关键错误帧 | 文件变更 | 强 |
| integrate | 全量回归 + 汇总 | 工作区 | 最终报告 | 中 |
| finalize | 产出报告、提交检查 | 全部状态 | 运行报告 | 无模型调用 |

**边与流转条件：**

| 起点 | 条件 | 终点 |
|------|------|------|
| plan | 任务树非空 | dispatch |
| plan | 抽取失败 | finalize（标记失败） |
| dispatch | 有待办任务 | implement |
| dispatch | 全部完成 | integrate |
| implement | 正常完成 | verify |
| verify | 通过 | dispatch |
| verify | 失败 且 retry_count < 上限 | diagnose |
| verify | 失败 且 retry_count ≥ 上限 | diagnose（标记该任务为高风险，仍尝试一次归因后跳过） |
| diagnose | 归因成功 | fix |
| fix | 完成 | verify |
| integrate | 通过 | finalize |
| integrate | 失败 且 预算充足 | diagnose |
| integrate | 预算耗尽 | finalize（标记失败并说明原因） |

**全局护栏（任一触发即熔断）：**

| 护栏 | 阈值（可配置） | 行为 |
|------|--------------|------|
| Token 上限 | 默认 800 万（按基线可调） | 暂停并提示 |
| 单任务步数上限 | 30 步 | 跳过该任务并标记 |
| 总墙钟上限 | 默认 6 小时 | 进入 finalize |
| 连续失败上限 | 3 次 | 跳过该任务 |

#### 5.3.4 工具层（M4）

| 工具 | 能力 | 超时 | 输出处理 | Token 风险 |
|------|------|------|---------|-----------|
| read_file | 读文件（支持行区间） | 5s | 超 800 行截断并标注 | 高 |
| write_file | 全量写文件 | 5s | 回执仅返回行数与 hash | 低 |
| apply_patch | 定向补丁（优先，避免全量重写） | 10s | 返回 diff 摘要 | 低 |
| list_dir | 列目录（带深度限制） | 5s | 过滤 node_modules 等 | 中 |
| glob_search | 文件名模式检索 | 5s | 上限 200 条 | 中 |
| grep_search | 内容检索（返回匹配行 + 上下文 2 行） | 10s | 上限 100 条 | 中 |
| exec_shell | 执行命令（白名单：npm/pnpm/node/git/lint/构建/测试） | 300s | 头尾各保留 200 行，中间折叠 | 高 |
| git_op | init / add / commit / diff | 30s | 返回精简摘要 | 低 |
| start_service | 启动本地服务并返回探测结果 | 60s | 返回启动状态与端口 | 中 |
| smoke_check | 冒烟探测（HTTP 请求 / 页面可达性） | 30s | 返回状态码与关键校验结果 | 中 |

**约束：** 命令白名单外一律拒绝；所有工具输出在进入上下文前必须经截断器处理；每次调用写入 trace。

#### 5.3.5 上下文与 Token 管理（M5）

| 策略 | 说明 | 优先级 |
|------|------|--------|
| 工具输出截断 | 头尾保留 + 中间折叠 + 标注"已省略 N 行" | P0 |
| 错误日志关键帧提取 | 只回传报错栈、失败用例名、断言差异，不回传整份日志 | P0 |
| 阶段性历史压缩 | 每完成一个任务，将该任务的对话历史压缩为摘要（"改了哪些文件、为什么、结论"） | P0 |
| 文件索引替代全文 | 上下文中只放文件路径 + 摘要 + 函数签名，需要时再精确读取 | P0 |
| 任务级上下文隔离 | 每个子任务只携带其依赖文件的必要片段，不携带全局历史 | P0 |
| 模型分级路由 | plan / implement / fix 用强模型；diagnose / integrate 用中模型；摘要压缩用弱模型 | P0 |
| Prompt 缓存 | 系统提示词与稳定上下文复用缓存（视网关支持） | P1 |

#### 5.3.6 验证与修复闭环（M6）

| 环节 | 说明 | 失败处理 |
|------|------|---------|
| 构建校验 | 执行项目构建命令，解析退出码 | 记录编译错误，进入 diagnose |
| 自测执行 | 执行测试命令，解析为结构化失败列表（用例名 + 断言 + 堆栈关键帧） | 同上 |
| 静态检查 | lint 与类型检查 | 仅记录，不阻断（P1） |
| 失败归因 | 模型判断责任文件与根因，输出结构化结论 | 归因失败 → 回传精简原始错误重试一次 |
| 定向修复 | 只加载责任文件相关上下文，生成最小改动 | 修复后重新 verify |
| 回归 | 全部任务完成后跑全量测试 | 失败且预算充足 → diagnose |

**硬规则：** 模型不得自述"已完成"即视为完成，必须有 verify 节点的结构化输出作为通过证据。

#### 5.3.7 模型网关（M7）

| 能力 | 说明 |
|------|------|
| 统一适配 | 通过 OpenAI 兼容 baseURL 接入赛事网关，支持 Claude / GPT / Kimi / DeepSeek 等 |
| 分级配置 | 强 / 中 / 弱三档分别绑定具体模型，可在控制台切换 |
| 降级链 | 主模型失败（超时/限流/报错）→ 自动切备用模型 → 事件留痕 |
| 计量 | 每次调用记录 prompt / completion token，按模型分类累加 |
| 并发控制 | 按模型配置最大并发，防止限流 |

#### 5.3.8 沙箱（M8）

| 能力 | 说明 | 优先级 |
|------|------|--------|
| 工作区隔离 | 每次运行在独立目录，预置模板项目 | P0 |
| 子进程执行 | 工具命令在受限子进程中执行，带超时与资源限制 | P0 |
| 环境变量隔离 | 不继承宿主敏感环境变量 | P0 |
| 网络策略可配置 | 允许 npm 源访问 | P0 |
| 容器化 | Docker Desktop / WSL2 镜像，与赛事沙箱环境对齐 | P1 |
| 环境快照 | 记录 Node 版本、依赖版本，保证可复现 | P0 |

#### 5.3.9 可观测与审计（M9）

**Trace 事件字段：**

| 字段 | 说明 |
|------|------|
| ts | 时间戳（毫秒） |
| run_id | 运行标识 |
| node | 当前图节点 |
| type | model_call / tool_call / verify / error / human / budget |
| name | 模型名或工具名 |
| input_tokens / output_tokens | Token 计量 |
| duration_ms | 耗时 |
| payload_ref | 大 payload 落盘后的文件路径（上下文中只放摘要） |
| status | ok / error / truncated |

**落盘：** 每次运行在 `runs/<run_id>/` 下产出 `trace.jsonl`、`tasks.json`、`report.md`、`diff/`，保证事后可完整复盘。

#### 5.3.10 提交检查（M10）

| 检查项 | 说明 |
|--------|------|
| 产物完整性 | 工作区文件齐全，无未提交的关键变更 |
| git 提交时间 | 提交时间戳连续且合理（评分依据之一） |
| 构建可复现 | 从干净目录重新 install + build 通过 |
| 自测报告 | 附带最近一次全量测试结果 |
| 运行报告 | 三项指标自评结果导出为 Markdown |

## 6 非功能需求

| 类别 | 要求 |
|------|------|
| 平台 | Windows 11 优先（宿主环境），兼容 macOS |
| 性能 | 控制台在高频事件流下 UI 不卡顿（事件批量推送，节流 100ms） |
| 稳定性 | Agent 主循环运行在 Electron 主进程；渲染进程崩溃不影响运行，可重连续看 |
| 资源 | 空闲时内存占用 ≤ 300MB；运行时峰值随并行度可控 |
| 安全 | 敏感配置（API Key）存本地加密文件，不写入 trace；命令白名单强制校验 |
| 可维护性 | 节点、工具、策略均为可替换模块；参数集中在配置文件 |
| 可复现 | 环境版本、模型版本、参数、prompt 模板版本全部记录进运行报告 |

## 7 数据模型

| 实体 | 关键字段 | 存储 |
|------|---------|------|
| Run | run_id、需求文档 hash、参数快照、环境快照、状态、开始/结束时间、总 token | `runs/<id>/run.json` |
| Task | 见 5.3.2 任务数据结构 | `runs/<id>/tasks.json` |
| TraceEvent | 见 5.3.9 | `runs/<id>/trace.jsonl`（追加写） |
| Artifact | 大模型输入输出原文（按事件 ID 分片） | `runs/<id>/payloads/<event_id>.json` |
| Report | 三项指标、失败清单、耗时分布、Token 分布 | `runs/<id>/report.md` |
| Config | 模型分级、并行度、预算、工具白名单、截断阈值 | `config/forge.config.ts` |

## 8 界面设计说明

| 项 | 说明 |
|----|------|
| 窗口 | 自定义标题栏（与主区一体化），窗口控件自绘；主区默认 1440×900 |
| 视觉 | 浅色为主，分区靠背景色阶 + 阴影 + 留白分层，不使用边框线条（沿用既有设计规范） |
| 主色 | 沿用品牌薄荷青 #4BD8BA，仅用于状态强调与操作按钮，主色上用深色文字 |
| 状态色 | 通过=绿、失败=红、运行中=品牌色、跳过=灰（语义色，非品牌色） |
| 字体 | 正文 13px / 行高 1.6；代码区等宽字体 |
| 刷新策略 | 时间线节流 100ms 批量渲染；Token 数字每 500ms 更新 |

## 9 里程碑与计划

| 阶段 | 时间 | 交付物 | 验收标准 |
|------|------|--------|---------|
| M0 基线 | 9/3–9/6 | 完成报名；用 Claude Code 跑一次练习赛题，实测三项基线值 | 拿到基线数字，写入 baseline.json |
| M1 骨架 | 9/7–9/13 | Electron + LangGraph 最小闭环：单任务"写代码→跑测试→修"，控制台能看到 trace | 本地小需求端到端跑通一次 |
| M2 成型 | 9/14–9/20 | 完整状态机 + 需求编译 + 上下文管理 + 成本面板 + 断点续跑 | 用练习赛题跑通，三项指标优于或等于基线 |
| M3 初赛 | 9/21–9/30 | 每日一次全量演练，按结果调优 | 进入排行榜 Top 20 |
| M4 大奖赛 | 10/1–10/7 | 增量变更需求支持（第二期范围） | 视晋级情况 |

## 10 风险与依赖

| 类型 | 风险 | 影响 | 应对 |
|------|------|------|------|
| 技术 | LangChain/LangGraph 抽象层引入额外 Token 开销 | Token 指标掉分 | 精简 prompt 模板；关键路径可绕开框架直接调用网关 |
| 技术 | 赛事沙箱与本地环境不一致导致本地跑通、线上失败 | 通过率归零 | M2 起尽量用 Docker 对齐；M0 阶段摸清沙箱规格 |
| 技术 | GUI 黑盒测试用例不可见，本地自测覆盖不到 | 通过率不达标 | 严格按需求文档逐条写验收断言；多轮演练反推 |
| 资源 | 并行策略导致 Token 超预算 | Token 指标掉分 | 默认串行，并行仅对低耦合任务开启，且受预算护栏约束 |
| 进度 | 研习营 9/7 才开课，9/21 初赛，开发窗口仅 2.5 周 | 交付不完整 | M1 严格收口在"能跑通"；先基线后优化，不做超前设计 |
| 外部 | 模型网关限流或额度不足 | 运行中断 | 多模型降级链 + 预算护栏 + Checkpoint 续跑 |

## 11 待确认决策点

| # | 决策项 | 选项 | 影响 |
|---|--------|------|------|
| 1 | 参赛形式 | 个人 / 2–3 人小队 | 决定是否投入人力做并行开发与调优分工 |
| 2 | 沙箱方案 | 本地子进程（快） / Docker 或 WSL2（对齐赛事） | 本地方案开发快但存在环境差异风险 |
| 3 | 正式比赛是否允许人工干预 | 允许 / 全自动 | 若全自动，控制台降级为只读观测，人工干预能力仅用于练习期 |
| 4 | 模型分级策略默认值 | 全强模型 / 混合 | 直接决定 Token 与质量的平衡点，需 M0 实测后定 |
| 5 | Checkpointer 选型 | 内存 / 文件持久化（LangGraph 官方 SqliteSaver） | 崩溃续跑与复盘需要持久化，倾向文件方案 |

## 12 术语表

| 术语 | 解释 |
|------|------|
| Harness | 包裹模型的工程框架：任务定义、工具、反馈、验收、经验沉淀的闭环 |
| Agent | 在 Harness 中执行具体动作的自主循环体 |
| 黑盒 GUI 测试 | 赛事方从外部调用产出的界面/接口验证功能，参赛者看不到用例实现 |
| Trace | 运行全过程的事件记录，用于复盘与审计 |
| Checkpoint | 状态机中间状态快照，用于崩溃后恢复 |
| Token 效率 | 完成同一任务消耗的模型 Token 总量，越少越优 |

## 13 参考

| 来源 | 链接 |
|------|------|
| 赛事官网 | https://create.gosim.org/factory26/ |
| 竞赛平台 | http://arc-bench.com |
| GOSIM 深圳 2026 大会 | https://shenzhen2026.gosim.org/ |
| LangGraph.js 文档 | https://langchain-ai.github.io/langgraphjs/ |

## 14 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-09-03 | 初稿，范围锁定初赛第一期 | 唐杰 |
